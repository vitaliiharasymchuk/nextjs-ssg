name: Test workflow

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Add your build, test, and deployment steps here
      # For example:
      - name: Modify next.config.js
        run: npm install replace -g
          replace "OLD_VALUE" "NEW_VALUE" next.config.js
          replace "ANOTHER_OLD_VALUE" "ANOTHER_NEW_VALUE" next.config.js
