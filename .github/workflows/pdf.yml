name: PDF test workflow

on:
  workflow_dispatch:

jobs:
  generate-pdf:
    runs-on: ubuntu-latest

    steps:
      - name: Install Puppeteer
        run: |
          sudo apt-get update
          sudo apt-get install -y chromium-chromedriver
          export CHROME_PATH=/usr/bin/chromium-browser
          
      - name: Generate PDF
        run: |
          /usr/bin/chromium-browser --headless --disable-gpu --print-to-pdf=generated.pdf --window-size=1920,1080 https://example.com
          
      - name: Upload PDF artifact
        uses: actions/upload-artifact@v2
        with:
          name: pdf-output
          path: generated.pdf          
